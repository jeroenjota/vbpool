h2.header Groepstanden  &nbsp;
  p.small  vul in 1/4  (#{weds.punten.groepstand}pnt per correcte invoer)
table.group(width="100%")
  tr
    each pool in pools.poules
      th Groep #{pool.code}
  each num in [1,2,3,4,5,6]
    tr
      each pool in pools.poules
        each team in pool.teams
          if team.number == num
            - var naam = pool.code + num
            td 
              label #{team.team}
                input(type="number" name=naam size="1" min="1" max="4" value="1")
